# Plan: team access requests (spec 13)

- [x] 1. Branch `feature/team-access-requests` from `origin/master` (`da70d9d`, #278/#280 merged). No outdated packages. Baseline build + full suite green (2,750).
- [x] 2. Contracts: `TeamAccessRequest`, `TeamAccessRequestStatus`, `TemporaryConsent`; `ITeam` default members. Tests on shape/defaults. `TeamAccessRequest` (+ status enum), `TemporaryConsent`, `TeamAccessRequestRules` (levels, history cap 20, message max 1000) in `Tharga.Team/TeamAccessRequest.cs`; `ITeam.TemporaryConsent`, `ITeam.AccessRequests` default null.
- [x] 3. Consent-in-force rule + tests (unexpired, expired → previous, expired with no previous → none, standing). Named `TeamConsent.Resolve(team, utcNow)` → `ConsentInForce(ConsentedRoles, AccessLevel, ExpiresAt)` with `Covers(roles)`; public (MongoDB and MCP need it). `TeamConsentTests` (13).
- [x] 4. Route every consent read through it: `TeamGrantResolver`, `TeamContextResolver`, `GetConsentedTeamsAsync` post-filter, `TeamVisibility`/`TeamComponent`/`TeamsListView`, MCP team resource. Architecture test forbidding direct reads elsewhere. Also the consent audit's old value. `TeamGrantResolver` re-checks `Covers(roles)` itself (a host's direct `ITeamService` may match on stored roles). Mongo consented-team query also matches `TemporaryConsent.PreviousConsentedRoles`; entity gained `TemporaryConsent`/`AccessRequests` (`TemporaryConsentEntity`, `TeamAccessRequestEntity`, enums by name, added to the enum sweep's self-check) — pulled forward from step 8 because the query needs them. `ConsentInForce.Roles` (renamed from `ConsentedRoles` so the guard can tell it apart). Two fixtures fixed: a "consented" fake team with no consented roles. `ConsentExpiryReadsTests` (7), `TeamVisibilityTests` +2. Suite 2,772 green.
- [x] 5. Security tightening: `SetTeamConsentAsync` requires `team:manage` held as a member; a direct change clears `TemporaryConsent`. Tests (consent-derived Administrator refused). `TeamScopePolicy.HasDirectTeamScope` (member key, or a team API key — not a system key through consent) + `TeamAuthorizer.HasDirectTeamScopeAsync`; decorator refuses with the reason. `TeamContext.MemberKey` + middleware claim so a member naming their team over REST is still direct. Mongo `SetConsentAsync` unsets `TemporaryConsent`. UI needed nothing: `CanEditConsent` already requires member-row Administrator. Fixtures updated (member key). `DirectTeamScopeTests` (10), `TeamRepositoryConsentTests` (2). Suite 2,784 green.
- [x] 6. Operations on `ITeamService` (default throwing members) + `TeamServiceBase` virtual persistence methods + rules (request/cancel/approve/deny). Tests first on the rules. Approve takes `consentedRoles` (the facade supplies `ConsentOptions.Roles`, as the consent selector does). Hooks: `AddAccessRequestAsync`, `DecideAccessRequestAsync` (conditional on pending → bool), `ApproveAccessRequestAsync` (one write). `TeamAccessApproval.Plan` computes window + previous consent (keeps the original previous during a running window). History cap changed to 50 total (`HistoryLimit`) — simpler for an atomic store push than "pending + 20 decided". `TeamAccessRequestTests` (19), `TeamAccessApprovalTests` (6); `TestTeamService` gained in-memory hooks.
- [x] 7. `AuthorizationTeamServiceDecorator` checks + `AuditingTeamServiceDecorator` entries; forward in every toolkit decorator. Tests. Request: `TeamAuthorizer.HoldsAnyRoleAsync(ConsentOptions.Roles)` (decorator gained optional `ConsentOptions`, passed by the Blazor registration). Approve/deny: direct `team:manage`; approved roles must be configured consent roles. Cancel passes through. Audit actions `request-access`, `approve-access-request`, `deny-access-request`, `cancel-access-request`; new `accessrequest.*` metadata keys; approval records old consent level, roles and granted-until (read back). Forwarding covered by the existing `DecoratorDefaultMemberTests`. `AccessRequestAuthorizationTests` (11), `AccessRequestAuditTests` (6). Suite 2,826 green.
- [x] 8. MongoDB: entity fields (enums by name), atomic approve update, decided-request cap. Tests incl. BSON representation. `ITeamRepository` default members throw; `TeamRepository`: decide/approve are one conditional positional update (`$elemMatch` on id + Pending, `AccessRequests.$`), approve sets consent + status + `TemporaryConsent` (or `$unset`) together; add cancels the requester's pending ones then `$push` with `$sort` RequestedAt desc and `$slice` 50. `TeamServiceRepositoryBase` overrides the three hooks. Embedded `Id` renders as `_id` (driver convention, harmless). `TeamRepositoryAccessRequestTests` (4, rendered shapes — matched path to verify against a real server in step 12), `TeamEntityAccessRequestSerializationTests` (2). Suite 2,832 green.
- [x] 9. Facades: `ITeamManagementService` members (gated) and new `ITeamAccessRequestService` (filtered), registered by the library with `TryAdd`. Registration + architecture tests. `ITeamManagementService`: `GetAccessRequestsAsync`, `ApproveTeamAccessRequestAsync(teamKey, id)` (passes `ConsentOptions.Roles`), `DenyTeamAccessRequestAsync` — default members that throw, `[RequireScope(team:manage)]`; the read is enforced in the facade (member holding manage). `ITeamAccessRequestService` (+ `TeamAccessRequestItem`): request, cancel, `GetMyAccessRequestsAsync` (all teams via `teams:read`, own only; empty without it), `GetAccessRequestsAwaitingMeAsync` (pending on own teams where the member grant has manage). Added to `TeamServiceFacets.All`, so registration and the completeness check pick it up. `TeamAccessRequestFacadeTests` (9). Suite 2,844 green.
- [x] 10. UI: request dialog + pending/cancel on non-member teams; manager approve/deny list; consent expiry display. Text catalogues. `TeamAccessRequestPanel` rendered at the top of `TeamComponent`'s shared `TeamDetail` (both layouts): temporary-consent notice, pending requests with Approve (confirm states "everyone with <roles>") / Deny for member managers of the selected team, Request access / Withdraw for non-members holding a consent role. `TeamAccessRequestDialog` (level select bar from `RequestableLevels` minus `HiddenAccessLevels`, duration choices 1h/8h/1d/1w/30d/no end, message). `TeamAccessRequestGate` (pure) + `TeamAccessRequestText` catalogue; both components Migrated in text coverage. `TeamComponent` loads the caller's own requests once per reload. `TeamAccessRequestGateTests` (18).
- [x] 11. `TeamNotificationMenu` + visibility/count logic tests; sample header placement. `RadzenProfileMenu` with a bell and a count badge = requests awaiting the caller; items list those first (newest first), then the caller's own waiting requests (uncounted). Choosing a request to decide selects its team (manage scope is issued for the selected team only) and opens the team page. Reloads on `TeamsListChangedEvent`. `TeamNotificationMenuModel` (pure) + `TeamNotificationMenuModelTests` (5); Migrated in text coverage; sample places it beside `<TeamSelector />`.
- [~] 12. Manual verification in the sample (two accounts ideally). Full suite.
- [ ] 13. Push for user testing.

## Notes

- Additive public API throughout (default interface members, virtual methods, new types). Hosts with their own `ITeamService` decorators must forward the new members (the #272 lesson) — release notes.
- Behaviour change: a consent-derived `team:manage` no longer changes consent — release notes.
- Docs when complete: new `docs/articles/access-requests.md`; implementation guide component table + consent section; README if it covers consent.
